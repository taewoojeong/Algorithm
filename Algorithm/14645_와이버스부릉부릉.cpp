#include <iostream>
using namespace std;

int main(int argc, const char * argv[]) {
    ios::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
    int N, K;
    cin >> N >> K;
    for(int i = 0; i < N; i++){
        for(int j = 0; j < K; j++){
            int num;
            cin >> num;
        }
    }
    cout << "비와이";
}
